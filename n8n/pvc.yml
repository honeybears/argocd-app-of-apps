apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: n8n-storage
  volumeName: n8n-pv
  resources:
    requests:
      storage: 10Gi